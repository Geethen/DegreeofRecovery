# Python
__pycache__/
*.py[cod]
*.egg-info/

# Regenerated by the pipeline scripts — see outputs/ for example artifacts
# from a sample-data run, which are checked in on purpose as a reference.
# Delete this line (or the outputs/ files) if you want a clean re-run to
# overwrite them in place.

# OS cruft
.DS_Store
Thumbs.db
